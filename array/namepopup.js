var person = prompt("Please enter your name", "Name");

if (person == null || person == "") {
    txt = "You don't have a name?";
} else {
    txt = "Hello " + person + "Do you see your name on the screen?";
}